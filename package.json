{
  "name": "cashbridgebot",
  "version": "2.0.0",
  "description": "Paystack + Telegram subscription bot with affiliate tracking",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "node -e \"require('./database/db').initDb().then(() => console.log('DB OK'))\""
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "sql.js": "^1.14.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
